# 项目经历 · 合同审查智能体（Contract Review Agent）

> 最终版简历文案 + 面试口述 + 追问弹药。所有数字均已逐条对齐代码与评测报告，可放心投递。
> 本文件只覆盖「合同审查项目」这一段；若需整合 WorkBuddy / EduAgent 等其它项目，可另行补充。

---

## 一、简历正文（推荐直接贴进简历）

**项目名称**：合同审查智能体（Contract Review Agent）　·　**角色**：独立开发

**技术栈**：LangGraph / FastAPI / Milvus / BGE-M3 / BGE-Reranker / DeepSeek / Vue3 / Docker

**项目背景**：针对人工审查合同耗时且易遗漏高风险条款（违约金超标、试用期违规），而纯 LLM 方案因幻觉导致法务不敢直接采信的痛点，构建「规则闸门 + RAG 精准检索 + 人工硬门控」的 6 节点审查流水线。

**技术亮点**：

- **构建 RAG 检索管线**：Milvus 混合检索（BGE-M3 Dense + Sparse）+ BGE-Reranker-v2-M3 精排，结合 HyDE / Multi-Query 改写提升口语化召回，支撑民法典 / 司法解释 / 裁判规则 / 示范条款四类知识库，口语化问题 recall@3 从 50% 提升至 75%

- **实现规则闸门 + 多维评审 + 人工审批闭环**：40 条确定性规则（违约金 ≤30%、试用期 ≤6 个月等）零成本前置拦截，四维度（法律 / 合规 / 财务 / 权责）LLM 并发评审，通过 LangGraph interrupt() + Command(resume) 实现审批暂停—恢复闭环；asyncio 全链路异步 + SSE 流式推送保障实时响应

- **基于 MCP 协议接入真实判例 + 工程兜底**：接入北大法宝 MCP 为高风险条款补充带真实案号的司法判例（高风险触发控制额度、失败静默降级）；三层兜底（重试 → 节点降级 → 系统兜底）配合 JWT 多租户所有权校验，保障可用性与数据隔离

**项目成果**：

- 口语化问题法条召回 recall@3 从 50% 提升至 75%，书面语稳定 100%（20 题黄金集）
- 40 条确定性规则零成本前置拦截核心合规项，不占用 LLM 调用
- 6 节点流水线全链路可降级，核心接口完成多租户所有权校验

---

## 二、展开版（可选，用于项目详情页 / 需要更详细描述的场合）

> 每段一个「解决 XX」主题，适合放在项目详情页或面试官索取详细项目说明时使用。

- **解决确定性风险漏检与 LLM 成本虚高**：设计「规则闸门前置」架构，构建 40 条确定性规则（违约金 ≤30%、试用期 ≤6 个月等）进行零成本毫秒级拦截，确定性违规项不进 LLM；串联 4 维度（法律 / 合规 / 财务 / 权责）LangGraph Send fan-out 并发 LLM 评审。

- **解决口语化表述与法条语义鸿沟**：构建 BGE-M3 Dense(1024)+Sparse 混合召回 + WeightedRanker 融合，经 BGE-Reranker-v2-M3 精排 Top-15；审查侧引入相邻法条上下文扩展，问答侧引入 HyDE 假设文档 + 多查询拆分。20 题法律黄金集上，口语化 recall@3 从 50% 提升至 75%，书面语稳定 100%。

- **解决 AI 幻觉导致法律意见不可采信**：在 retrieve 后植入 LangGraph interrupt() 人工审批硬闸门，支持确认 / 降级 / 忽略三态，高敏意见经法务确认后方生成修订；接入北大法宝 MCP 为高风险条款补充带真实案号的司法判例，离线库失败时静默降级。

- **保障服务不中断与企业数据安全**：为 parse / review / retrieve / revise 节点设计正则兜底 / 综合评审 / 人工核实 / 起草占位 4 类节点降级，配合统一三层兜底（重试 → 降级 → 系统兜底）；通过 JWT 解析用户身份并贯通 10 个核心接口的所有权校验，阻断多租户越权访问。

---

## 三、STAR 面试口述版（每段 30 秒可背）

**开场**：这个项目我做的是一个合同审查 Agent。背景是人工审一份 30 页合同要半小时以上，违约金超 30%、试用期超 6 个月这种硬伤人眼容易漏；但直接丢给大模型又会编造法条，法务不敢信。所以我的核心思路是三句话——规则闸门先拦确定性风险、检索佐证补法条依据、人工门控把住最后一道关。

**模块一 · 规则闸门 + 四维并发**：我没有一上来就把合同丢给大模型，而是先加一道纯规则闸门：用正则写 40 条确定性规则，违约金超 30% 直接标红、试用期超 6 个月报高风险，毫秒级、零 Token。确定性规则能判的事就不让 LLM 重复做。之后语义性审查才交给 LLM，法律 / 合规 / 财务 / 权责四维度用 LangGraph Send 四路并发。

**模块二 · 混合检索 + 查询增强（主菜）**：合同里用户的话和法条是两个语言体系——问「仓库被雷劈了货烧了我还要赔吗」，法条写的是「不可抗力」，纯向量检索零召回。我做了三层：BGE-M3 dense+sparse 混合召回加 WeightedRanker 融合；BGE-Reranker 交叉编码器精排取 Top-15；口语化查询增强，问答侧加 HyDE 让 LLM 先生成「假设法条」再检索。效果是 20 题黄金集上口语化 Top-3 命中率 50% 提升到 75%，书面语 100%。

**模块三 · HITL 人工门控**：就算检索到真法条，大模型还是会胡编，所以我加一道人工硬门控：检索之后、生成修订之前，用 LangGraph interrupt() 暂停流水线，把高风险项推给前端，让法务确认 / 降级 / 忽略三选一，确认后用 Command(resume) 恢复。另外接了北大法宝 MCP，高风险条款自动补带真实案号的判例，对抗编案例，离线库挂了静默降级。

**模块四 · 工程兜底 + 数据安全**：每个节点都有降级——解析挂了退回正则切分、评审挂了退化成单次综合评审、检索挂了标记人工核实、修订挂了生成「需人工起草」占位，统一套「重试→降级→系统兜底」三层保护。安全上主动修过多租户越权，用 JWT 解析身份，把 10 个核心接口所有权校验全补上，防止用户 A 读到用户 B 的合同。

**收尾**：检索这块有可量化提升，口语化命中率 50% 到 75%；架构上规则闸门、四维并发、人工门控、全链路降级都真正落地，不是 demo，而是有单测和评测脚本覆盖的。

---

## 四、追问弹药（投前必读，面试高频）

1. **「40 条规则」最难的是哪条？** → 违约金抽取。要区分「违约金 35%」要拦、「违约金不超过 30%」不能拦，靠正则同时抽百分比和固定金额再跟 30% 阈值比较，不是简单关键词匹配。

2. **「recall@3 50%→75%」什么口径？** → 口语化子集（4 题）的 recall@3；50% 是 HyDE 关闭（A/B 对照）的基线，75% 是加 HyDE 后的值，测试集是 20 题法律黄金集。

3. **HyDE 触发阈值为什么是 0.80？** → 因为硬截断已把 <0.30 的命中滤掉，「top<0.3 触发」会永远不触发；阈值落在口语化弱命中（≤0.79）与强命中（≥0.91）之间的空档。

4. **interrupt 后状态存哪、怎么恢复？** → MemorySaver checkpointer + thread_id（`contract_{id}_run_1`），resume 时用 Command(resume) 把法务决策作为 payload 传回。图路径做真暂停、直跑路径做逃生舱。

5. **为什么是 6 节点而非多 Agent？** → 合同审查是单业务流水线，用 StateGraph 6 节点（parse→rule_check→review→retrieve→human_gate→revise）+ review 内部 Send fan-out，比拆成多个独立 Agent 更可控、状态更清晰。

---

## 五、数字真实性备忘（自己心里有数）

| 简历写法 | 代码事实 | 出处 |
|---|---|---|
| 6 节点流水线 | parse→rule_check→review→retrieve→human_gate→revise | graph.py |
| 40 条规则 | rule_id R001~R804（R302 空缺，含租赁/买卖/服务专项） | rule_engine.py |
| 4 维度并发 | legal/compliance/financial/rights_obligations，Send fan-out | multi_dim_review.py |
| Dense(1024)+Sparse + WeightedRanker(0.7/0.3) | EMBEDDING_DIM=1024 | core/rag.py |
| Reranker-v2-M3 精排 Top-15 | RERANK_POOL=15 | core/rag.py |
| recall@3 50%→75% / 书面语 100% | 口语化子集，20 题黄金集 | outputs/eval_*.md |
| interrupt()/Command(resume) | wait_human_node | graph.py |
| 10 个接口所有权校验 | _check_ownership + 各处 user_id 判断 | api/v1/contract.py |
| 三层兜底 + 4 节点降级 | retry.py「重试→降级→系统兜底」 | core/retry.py |
| 北大法宝 MCP | 高风险触发、失败静默降级 | mcp/pkulaw_client.py |
